<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- طبقة الخلفية الحية -->
    <com.alol.overlay.ui.LiquidSurfaceView
        android:id="@+id/liquidBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- طبقة الأزرار الزجاجية -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:padding="24dp"
            android:paddingTop="60dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="LEGION OS"
                android:textSize="38sp"
                android:textColor="#00FFD1"
                android:textStyle="bold"
                android:shadowColor="#00FFD1"
                android:shadowRadius="25"
                android:layout_marginBottom="6dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Shadow Command Center"
                android:textSize="14sp"
                android:textColor="#80FFFFFF"
                android:layout_marginBottom="36dp" />

            <Button android:id="@+id/btn_acvs" style="@style/GlassButton" android:text="👁️ العين الإلهية (ACVS)" />
            <Button android:id="@+id/btn_eta" style="@style/GlassButton" android:text="🌐 مُحلل حركة المرور (ETA)" />
            <Button android:id="@+id/btn_vse" style="@style/GlassButton" android:text="🧪 بيئة الاختبار الافتراضية (VSE)" />
            <Button android:id="@+id/btn_rav" style="@style/GlassButton" android:text="🛡️ الحماية الذاتية (RAV)" />
            <Button android:id="@+id/btn_ued" style="@style/GlassButton" android:text="📚 قاعدة بيانات الثغرات (UED)" />
            <Button android:id="@+id/btn_iga" style="@style/GlassButton" android:text="🧠 المساعد الذكي (IGA)" />
            <Button android:id="@+id/btn_dae" style="@style/GlassButton" android:text="💰 سوق الظل (DAE)" />
            <Button android:id="@+id/btn_cbg" style="@style/GlassButton" android:text="🤖 مصنع العباقرة (CBG)" />
            <Button android:id="@+id/btn_per" style="@style/GlassButton" android:text="💓 مُحلل المشاعر (PER)" />
            <Button android:id="@+id/btn_ibp" style="@style/GlassButton" android:text="♾️ بروتوكول الخلود (IBP)" />

        </LinearLayout>
    </ScrollView>
</FrameLayout>
